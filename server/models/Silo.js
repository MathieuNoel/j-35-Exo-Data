const SiloTemplate = {
    level: 0,
    capacity: 30,
    cost: 15
};

const Silo = {
    upgrade: {
        CAPACITY_FACTOR: 1.3,
        COST_FACTOR: 1.3
    },
    generate: (level) => {
        // tout pareil
    }
};

module.exports = Silo;